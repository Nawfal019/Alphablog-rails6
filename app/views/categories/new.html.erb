<h1 class="text-center mt-4">Add new Category for Alpha Blog</h1>

<div class="container">
  <%= render 'shared/errors' , obj: @category%>
  <div class="row justify-content-center">
    <div class="col-10">
      <%= form_with(model: @category, class: "shadow p-3 mb-3 bg-info rounded", local: true) do |f| %>
        <div class="form-group row">
          <%= f.label :name, "Category Name", class: "col-2 col-form-label text-light" %>
          <div class="col-10">
            <%= f.text_field :name, class:"form-control shadow rounded", placeholder: "Type your name" %>
          </div>
        </div>

        <div class="form-group row justify-content-center"> 
          <%= f.submit "Submit" ,class: "btn btn-outline-light btn-lg" %>
        </div>
      <% end %>
    </div>
    <div class="mb-3">
      <%= link_to '[ Cancel and Go Back to All Articles ]', articles_path, class: "text-info" %> </td>
    </div>
  </div>
</div>